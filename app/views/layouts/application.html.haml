!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title.text JetRuby
    = csrf_meta_tags
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
  %body
    .container
      .header.clearfix
        %nav
          %ul.nav.nav-pills.pull-right
            - if user_signed_in?
              %li{:role => "presentation"}= link_to 'Sign Out', '/users/sign_out', method: :delete
              %li{:role => "presentation"}= link_to 'Edit profile', '/users/edit'
              - if current_user && current_user.lunchesadmin?
                %li{:role => "presentation"}= link_to 'Registered users', '/regusers'
            - else
              %li{:role => "presentation"}= link_to 'Log In', '/users/sign_in'
              %li{:role => "presentation"}= link_to 'Register', '/users/sign_up'
        %h3.text-muted.pull-left JetRuby
      - if notice
        .alert.alert-success{:role => "alert"}= notice
      - if alert
        .alert.alert-warning{:role => "alert"}= alert
      = yield
